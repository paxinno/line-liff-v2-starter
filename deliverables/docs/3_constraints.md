# 既知の制約・注意点

- liff.sendMessages は **ユーザーがLIFFを開いている前提**。バックグラウンド送信は不可。
- Messaging API は **サーバ側トークン**が必要で、友だち関係・権限設定が前提。
- 端末・LINE設定により、LIFFが外部ブラウザで開く可能性（挙動差に注意）。
- 公開リポのため、個人名／秘匿情報は禁止。すべて環境変数で注入。
- Netlify の Node バージョンは **20以上**推奨（ @line/bot-sdk の engine 要件）。